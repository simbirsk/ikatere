<%= simple_form_for @location do |f|%>
	<%= f.hidden_field :user_id, value: current_user.id %>
	<%= f.input :name %>
	<%= f.input :address %>
	<%= f.input :latitude %>
	<%= f.input :longitude %>
	<%= f.button :submit %>
<% end %>