<% if user_signed_in? %>
  <li>
  	<%= link_to('Profile', edit_user_registration_path) %>
  </li>
<% else %>
  <li>
	<%= link_to "Register", "/", "data-toggle" => "modal", "data-target" => "#registerModal" %>
  </li>
<% end %>